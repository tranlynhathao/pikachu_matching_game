#pragma once
#include "Lib.h"
#include "Struct.h"

// The function has the function of reading and writing from the file saving game results accordingg to the list of top 5 fastest game completions of all 3 modes.
void readFile(string name);
void inputAndPrintFile(string namef, char a[100], int time);